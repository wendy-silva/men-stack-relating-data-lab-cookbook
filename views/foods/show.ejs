<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= food.name %></title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  <h1><%= food.name %> </h1>
  <!-- We'll use method-override to allow us to hit a delete route: -->



  <a href="/users/<%= user._id %>/foods/<%= food._id %>/edit">
    Edit this Food
  </a>
  <form
    action="/users/<%= user._id %>/foods/<%= food._id %>?_method=DELETE"
    method="POST"
  >
    <button type="submit">Delete this FOOD from the PANTRY</button>
  </form>
  
</body>
</html>